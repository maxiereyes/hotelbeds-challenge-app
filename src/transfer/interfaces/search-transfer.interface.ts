export interface SearchTransfer {
    language: string,
    fromType: string
    toType: string
    fromCode: string
    toCode: string
    outbound: string
    adults: number
    children: number
    infants: number
}